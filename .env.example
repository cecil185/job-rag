# OpenAI
OPENAI_API_KEY=sk-...

# Database (local docker default; override with Neon connection string in prod)
DATABASE_URL=postgresql://jobrag:jobrag_password@postgres:5432/jobrag_db

# Auth: shared passcode that gates the deployed alpha
APP_PASSCODE=changeme
